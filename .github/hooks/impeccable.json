{
  "version": 1,
  "hooks": {
    "postToolUse": [
      {
        "type": "command",
        "bash": "node \"$(git rev-parse --show-toplevel)/eng/hooks.mjs\" --provider copilot --event edit",
        "powershell": "$novaHookRoot = git rev-parse --show-toplevel; if ($LASTEXITCODE -eq 0) { & node (Join-Path $novaHookRoot 'eng/hooks.mjs') --provider copilot --event edit }",
        "timeoutSec": 10
      }
    ]
  }
}
