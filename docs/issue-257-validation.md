# Issue 257 validation

Scope: [#257](https://github.com/eruvalca/Nova/issues/257), the in-app Closed campaign record. One PR on `codex/issue-257-closed-record`, based on `199b58b3` (#281). The user approved inline participant history and search/outcome discovery. `gh-stack` and its stack-design guidance were reviewed: this is one coherent UI/read-contract change with no useful independent prerequisite, so no stack is used.

## Contract and boundaries

- The existing Closed roster query returns local final decisions, live player/team archive metadata, whole-campaign outcome totals and the latest stored closing event inside its existing repeatable-read transaction. Integrity precedes filters. Each response has its own snapshot; separate pages and history/activity requests do not claim one shared snapshot.
- The Close board uses 50-row search/outcome discovery, inline campaign-only history in 20-event pages, and up to 50 recent close/reopen events. Native GET forms and links preserve sibling URL context; interactive attachment restores owned startup success/error state. Regions retry independently and stale owners cannot publish.
- `CampaignLifecycleActions` remains the only lifecycle command owner. A Closed-only history request conflicts after reopen. Existing evaluation navigation is read-only and returns to Close.
- No schema, dependencies, Sass, runtime configuration, export, printing or second activity store. Original decisions/actor snapshots remain historical; player/team display metadata are not new immutable profile snapshots. The import-error sentence in the intake brief remains owned by #218, as #257 explicitly directs; product and journey scope now exclude that download and retain import/template/in-app review.

## Guidance actually read

- [AGENTS.md](../AGENTS.md), [.github/pull_request_template.md](../.github/pull_request_template.md), PRODUCT.md, DESIGN.md, existing #256 Closed capture and validation, current closeout/campaign-spine briefs, and the [scoped direction](../.impeccable/surfaces/issue-257-closed-record.md).
- `.github/instructions/`: csharp-conventions, blazor-architecture, ui-design, bootstrap-theme, placement-decisions, season-lifecycle, service-layer, validation, api-endpoints, ef-core-tenancy, functional-core and testing instructions.
- `.agents/skills/`: add-feature-slice with input-validation/service-result/WASM-client references; add-api-endpoint with endpoint/client/contract references; add-blazor-ui with placement/render-mode/lifecycle-state references; nova-testing with transition, bUnit, SQLite, Aspire integration and browser-suite references; impeccable with shape/new-work/craft-floor/operate references.
- Installed `gh-stack` skill and stack-design; dotnet-test run-tests, filter-syntax, code-testing-agent and unit-test-generation prompt; find-untested-sources. Roslyn pairing completed once (566 source/387 test files); research/plan and raw pairing live in the worktree's Git-private `testagent` scratch directory. No coverage percentage is claimed.
- Focus follow-up additionally applied add-blazor-ui's JavaScript interop reference and the focused code-testing-agent workflow; run-tests overlay was absent. Render-mode ownership remains the existing InteractiveAuto campaign host.
- PR review and fix follow-up: installed code-review skill with PR/local review procedures, doctrine and checklist; test-anti-patterns with test-analysis-extensions and its .NET reference. The existing feature/API/Blazor/testing recipes above also govern these corrections.

## Current validation

Original implementation: `b17ba2001710a9c7304330dbe23019b52f93fc7a`, followed by documentation-only evidence stamp `b29774847feac70ebba3982bb5d8ed97e7292975`. The first review-fix validation tested that latter revision plus the seven then-uncommitted application/test paths: `HttpEffectivePlacementQueryService.cs`, its `.ClosedRecord.cs` tests, `CampaignWorkspace.Close.cs`, `CampaignWorkspace.Evaluation.cs`, `CampaignWorkspace.razor`, `CampaignEvaluationPanel.razor.cs`, and `CampaignClosedRecordBrowserTests.cs`. Those tested inputs were committed as `e1fc09bc41f986906f7afa1971b7a7d54acf2ac5`. The subsequent guidance follow-up below changes documentation only. At the review-fix push, remote `main` still equaled the base revision; no incoming changes were added.

Current focus-fix validation uses `30adebc9b47c13b7b5bc14f235dac8ffabba3411` plus four application/test paths: `CampaignClosedRecord.razor.cs`, `CampaignClosedRecordTests.cs`, `CampaignClosedRecordBrowserTests.cs` (partial declaration only), and new `CampaignClosedRecordFocusBrowserTests.cs`. The validation-record edits are documentation only. The 1,226-file application/test/build/generated-input inventory has SHA-256 `4eee5fc97f8c84fcf441cd5ff203cb6d0d960581c8a46e853fa0c1886de3caf6`; every entry remains unchanged after browser validation and the final documentation update. No incoming application changes were incorporated.

**Draft PR; merge remains blocked on the unexplained modified-click validation incident.** The previous full browser run passes, but that does not resolve the original failure or cover the new focus fix. Full browser validation of the final inputs remains required before merge. No merge is requested or performed.

The review-fix inventory contains 1,225 application/test/build inputs, including generated theme CSS; its JSON file SHA-256 is `c092ed771ee085f3ba0b6da393d4c92e738938f62aeabc6f342a500945d5593a`. All entries still match after the passing full browser run. Compared with the original final browser inventory, exactly the seven paths above changed. Original inventories remain `1151abba52a0ad7547a61bc6e4a7d8f441e6870e1fa5dc09e0960f2985709146` (initial unit/selected browser) and `f3efcc288958ba8fa25ec6c0c4fcc058a0145a4633e48dede9a919681e314caf` (diagnostic browser addition); those runs and curated captures identify the original implementation, not fresh evidence for these fixes.

| Check | Command / evidence | Current result |
| --- | --- | --- |
| Build | `dotnet build Nova.slnx` | Focus fix: pass, 0 warnings/errors |
| Format | `dotnet format Nova.slnx --verify-no-changes` | Focus fix: pass, exit 0 |
| Focused component | `dotnet test --project Nova.Unit.Tests/Nova.Unit.Tests.csproj --no-build --filter-class '*CampaignClosedRecordTests'` | Focus fix: 14 passed, 0 failed/skipped |
| Full unit | `dotnet test --project Nova.Unit.Tests/Nova.Unit.Tests.csproj --no-build` | Focus fix: 3,596 passed, 0 failed/skipped; later edits only normalized file encoding and narrowed browser fault injection |
| Full integration | `dotnet test --project Nova.Integration.Tests/Nova.Integration.Tests.csproj --no-build` | Review fixes, including final Undecided guard: 630 passed, 0 failed/skipped |
| Selected browser | `dotnet test --project Nova.Browser.Tests/Nova.Browser.Tests.csproj --no-build --filter-class '*CampaignClosedRecordBrowserTests' '*CampaignCloseBrowserTests'` | Focus fix: 12 passed, 0 failed/skipped |
| Full browser | `dotnet test --project Nova.Browser.Tests/Nova.Browser.Tests.csproj --no-build` | Previous `e1fc09bc`: 201 passed, 0 failed, 8 existing opt-in capture skips; does not cover the new focus fix; final full run pending |
| Modified-click diagnosis | `dotnet test --project Nova.Browser.Tests/Nova.Browser.Tests.csproj --no-build --filter-method '*ModifiedPlayerClickOpensNewTabWithoutChangingOriginalDraftAsync'` | Original implementation: 1 passed, 0 failed/skipped; diagnostic evidence only |
| Independent local review | [Original review artifact](../.impeccable/review/issue-257/local-code-review.md); review-fix dispositions below | Focus fix reviewed separately; browser interception correction verified; no remaining finding by source inspection |
| Visual finish/documentation | [Finish review](../.impeccable/review/issue-257/finish-review.md), [capture manifest](../.impeccable/review/issue-257/capture-manifest.json), DESIGN.md and `.impeccable/design.json` | Independent **ship (scoped PASS)**; scoped documentation merged and validated |

Aspire suites run serially, with each fixture starting its own AppHost. For the first review fixes: Source and generated inputs remain fixed during browser runs. No separately started AppHost is used. Full browser validation is selected because shared evaluation links and native form context changed; the original full pass cannot establish the new round trip. The existing capture-only jobs remain opt-in, with their environment flags unset; no new skip or weakened check was added. Original curated captures and visual finish are reused for unchanged visual composition; these fixes change URL preservation and invalid-response rejection only.

For the focus fix's intermediate push, the browser selection is `CampaignClosedRecordBrowserTests` (record/history/navigation and new failure/retry DOM focus) plus `CampaignCloseBrowserTests` (the lifecycle refresh host, close/reopen/reclose and regional failure siblings). The change is confined to the Closed component's post-render focus guard; it does not affect provider, service, HTTP or EF behavior, so the full integration pass at `e1fc09bc` is reused. No markup, styling or layout changed; existing visual evidence remains applicable. The earlier full browser pass is retained as historical evidence, not reused as final-input coverage.

## PR review fixes

- [Summary contradiction finding](https://github.com/eruvalca/Nova/pull/282#discussion_r4027488990): fixed by bounding each returned outcome count and each outcome-filtered total by its whole-campaign bucket. `ClosedRecordRejectsBalancedSummaryContradictingReturnedOutcomeAsync` covers balanced redistribution for Assigned, Not selected and Withdrawn. `ClosedRecordBoundsFilteredTotalWithoutEquatingPageOrSearchToWholeSummaryAsync` rejects oversized filtered totals while accepting equal and smaller totals with one-row pages. Independent reviewer `closed_record_review` identified the allowed Undecided filter's empty-page sibling; it now uses the already-zero Undecided bucket, with positive-total rejection and zero acceptance in `ClosedRecordBoundsUndecidedTotalEvenOnEmptyOutOfRangePageAsync`. The reviewer re-inspected the guard, page contract, base fixture and paired controls and confirmed the finding resolved by source.
- [Place-context handoff finding](https://github.com/eruvalca/Nova/pull/282#discussion_r4027489292): fixed by carrying namespaced Place discovery, selected participant and return context through the Closed evaluation link, evaluation tab, lookup links and native search form. Deliberate Place-player handoffs still choose their own Place state/participant and bypass this callback; the independent reviewer verified that sibling. `EvaluationRoundTripPreservesPlaceContextThroughSearchPagingAndSelectionAsync` exercises Close → evaluation → search → page → selected player → Close with JavaScript both enabled and disabled, checking decoded query values and exactly one occurrence per carried key. Final browser result is recorded above.
- The separate reviewer inspected the complete fix diff, intended behavior, constraints and test evidence, made no edits and ran no tests, and found no remaining concrete defect after the Undecided correction. Execution results are from the implementing agent. The earlier R3 modified-click incident remains open in the original review artifact; these dispositions do not resolve it.
- [Detached history-heading finding](https://github.com/eruvalca/Nova/pull/282#discussion_r4028313652), from the [second PR review](https://github.com/eruvalca/Nova/pull/282#pullrequestreview-5225390585): focus now requires the same rendered-record and selected-participant conditions as the heading; hidden branches clear the stale element reference, and removing selection cancels pending focus. `RefreshFailureDefersHistoryFocusUntilRecordRetryRendersHeadingAsync` proves no focus call for immediate and delayed record failure, followed by focus on successful regional retry. Both rows failed on the original guard at the unexpected focus-call assertion. `RemovingSelectionDuringRefreshDiscardsPendingHistoryFocusAsync` checks removal and subsequent reselection. `FailedRecordRefreshKeepsHistoryRetryableAndFocusesRestoredHeadingAsync` exercises the real WASM lifecycle refresh, injected main-record 500, retry, focus and history paging. Independent reviewer `closed_record_pr_ui_review` confirmed the guard and unit cases; it identified that the initial browser route also intercepted hidden Roster reads. The route now targets only `sortBy=closeout` without `participantId`, which the reviewer re-inspected and confirmed. `CampaignCloseRoster` has no analogous focus call; `CampaignLifecycleActions` renders its heading unconditionally. No broader exception catch, assertion relaxation or retry-policy change was added.

## Behavioral evidence

| Requirement | Named evidence |
| --- | --- |
| Original local outcomes versus later effective decisions | `ClosedRosterPreservesLocalOutcomeTeamAttributionAndTokenAfterSupersessionAsync`; existing effective-placement query and consumer tests |
| Whole totals, stored closer and archived context | `ClosedRecordKeepsWholeTotalsAndOriginalCloserWithArchivedFilteredRowsAsync`; `FinalRecordShowsOriginalEvidenceArchiveContextAndIndependentTotals` |
| Integrity before discovery and strict client metadata | `MissingClosureIsIntegrityConflictEvenWhenDiscoveryWouldReturnNoRowsAsync`; existing incomplete-decision discovery tests; `ClosedRecordRejectsMissingRequiredSnapshotFieldsAsync`; `ClosedRecordRejectsContradictoryMetadataAsync` |
| Coherent lifecycle/decisions/totals/closing attribution per response | Extended `ClosedRosterSnapshotRetainsClosedLifecycleAndDecisionWhenReopenedBetweenReadsAsync` in PostgreSQL suite |
| Closed-only history with no Active broadening | `ClosedOnlyHistoryRejectsActiveCampaignWithoutBroadeningScopeAsync`; `ActiveHistorySpansSavedParticipationsWhileClosedHistoryKeepsItsCampaignAsync`; real HTTP 409/ProblemDetails guard in `PlacementContextHttpTests.AssertClosedContextGuardAsync` |
| Native filters, off-page selection, history cursor and evaluation handoff | `NativeAndInteractiveDiscoveryPreserveSiblingsAndResetSelection`; `SelectedHistoryUsesExactParticipantAndClosedScopeWithNativeCursorAndEvaluation`; both `CampaignClosedRecordBrowserTests` journeys |
| Independent failure/retry, empty/stale page and integrity display | `ActivityAndHistoryFailuresRetryIndependentlyOfVerifiedRecord`; `EmptyResultsDistinguishCampaignFilterAndStalePage`; `IntegrityFailureHidesFinalEvidenceAndDoesNotCreateRefreshLoop` |
| Async ownership and prerender/interactive restore | `LateRecordFromPreviousOwnerCannotReplaceNewOwnerAsync`; `MatchingPersistedSuccessOrFailureSkipsDuplicateStartupReads`; parent-composed `DelayedHistoryDenialAfterUnchangedParentRefreshSettlesAndRemainsRetryableAsync`; existing workspace authority and lifecycle suites |
| Reopen removes final view, reclose refreshes, sole command owner | Extended `AdministratorCancelsThenClosesAndReopensWithFocusAndRetainedOutcomesAsync`; existing lifecycle policy/service/retry/component suites |
| Member visibility and inaccessible/Draft/Active routes | Existing effective-placement query/service HTTP authorization tests and Closed readability suite; member browser journey |

## Material failure dispositions

- Focus regression setup: the first build rejected synchronous `Click` in an async test and an implicit string comparison; `ClickAsync` and ordinal `string.Equals` corrected the tests without suppressions. The two intended pre-fix regression failures reproduced focus on an absent heading during loading and after failure; the new guard is confirmed by the focused and full unit passes above. Format verification found missing UTF-8 BOM encoding in the new browser file and edited unit file; targeted `dotnet format whitespace Nova.slnx --include Nova.Browser.Tests/CampaignClosedRecordFocusBrowserTests.cs Nova.Unit.Tests/Campaigns/CampaignClosedRecordTests.cs` normalized encoding.
- Review-fix builds flagged nested conditional expressions and lowercase normalization in a test; early return/switch expressions and uppercase case-insensitivity coverage satisfy the existing analyzers. Format verification flagged test imports and initializer spacing; the targeted `dotnet format Nova.slnx --include Nova.Unit.Tests/Campaigns/HttpEffectivePlacementQueryServiceTests.ClosedRecord.cs` correction is followed by a clean build, 3,593-case unit pass and full format verification. No analyzer, assertion or validation rule was suppressed.
- Initial build: Razor parsed `@page` as a directive; explicit expressions corrected it. Analyzer findings required documented disposal-only focus catches, extraction of reopen URL cleanup, and a safe source-validation ordering. Follow-up build passed.
- New test build: explicit `ServiceResult` payload types, nullable assertions, ordinal string comparisons and async test APIs corrected fixture compilation. No production checks or analyzers were suppressed.
- Initial multi-value class selection ran zero tests; this is not a pass, and its cause was not established. A single exact class selection passed all 11 new component cases. Subsequent runner verification at `e1fc09bc41f986906f7afa1971b7a7d54acf2ac5` disproves attributing the zero-test result to repeated `--filter-class`: single-flag/multiple-value and repeated-flag discovery both select the same 158 tests, and repeated-flag execution passes all 158. The full suite remains the authoritative application result; no filter restriction is added. Exact verification commands are recorded below.
- Initial full unit run: 3,571 passed / four failed. The missing-closure fixture incorrectly attempted to delete append-only events; it now creates an otherwise Closed campaign without a closing event. Two numeric-sort cases retained an Assigned team archive value after switching the row to Not selected; fixtures now update team state and totals coherently. The directory link expectation now matches the absolute route. All four pass in the final full unit run. No application behavior was relaxed.
- The post-review unit run passed 3,578 cases, including the parent-composed delayed-denial regression, and failed one new client test because its UnixEpoch fixture violates existing history validation. Changing only that fixture to UnixEpoch plus one day preserves the foreign-campaign/Closed-only assertions. The final full run passes all 3,579 cases.
- Initial format verification found whitespace in four updated fixture files. `dotnet format whitespace Nova.slnx --verbosity minimal` applied the corrections; the subsequent build and final format verification pass. Missing fixture imports and a test helper exceeding the statement limit were also corrected before the passing build.
- The first full browser run passed 198 behavior cases and failed only `ModifiedPlayerClickOpensNewTabWithoutChangingOriginalDraftAsync`, with eight existing opt-in capture skips. The isolated diagnostic case and complete rebuilt suite subsequently pass. The original failure is still unexplained: see the separate review's R3 incident/disposition. No contention or hydration cause is inferred, and no retry, timeout, assertion or skip was changed.

## Guidance follow-up

Scope: documentation-only follow-up to `e1fc09bc41f986906f7afa1971b7a7d54acf2ac5`. Following the [instructions-hygiene guidance](https://devblogs.microsoft.com/dotnet/instructions-hygiene-what-frontier-models-still-need-you-to-say/) and installed skill-creator skill, the follow-up clarifies workspace query ownership in the existing Blazor rules, extends the existing URL-transition evidence row, and documents absent closing-event normalization in the shared campaign seed reference. Integration and browser references link to that seed guidance. `AGENTS.md`, command/filter guidance, skill entrypoints, custom agents, and test gates remain unchanged.

Focused self-review checked the guidance against the implemented navigation round trip and seed interceptor: preservation is limited to the same authorized workspace and permits intentional resets; valid Closed fixtures need explicit evidence while integrity-test fixtures remain invalid. The shared `.agents/skills/nova-testing` source serves both Codex and Copilot; it has no separate `.github/skills` copy. `python C:/Users/eruva/.codex/skills/.system/skill-creator/scripts/quick_validate.py .agents/skills/nova-testing` passes. Relative Markdown targets resolve, the campaign-lifecycle heading exists, and `git diff --check` passes. These checks verify structure and consistency, not a new agent-behavior trial.

The preceding assessment verified the installed runner at the same application revision with:

| Command | Result |
| --- | --- |
| `dotnet test --project Nova.Unit.Tests/Nova.Unit.Tests.csproj --no-build --help` | Pass; class filtering supports multiple values with OR semantics |
| `dotnet test --project Nova.Unit.Tests/Nova.Unit.Tests.csproj --no-build --list-tests --filter-class '*CampaignClosedRecordTests' '*HttpEffectivePlacementQueryServiceTests'` | 158 discovered: 11 component cases and 147 client cases |
| `dotnet test --project Nova.Unit.Tests/Nova.Unit.Tests.csproj --no-build --list-tests --filter-class '*CampaignClosedRecordTests' --filter-class '*HttpEffectivePlacementQueryServiceTests'` | Same 158 discovered |
| `dotnet test --project Nova.Unit.Tests/Nova.Unit.Tests.csproj --no-build --filter-class '*CampaignClosedRecordTests' --filter-class '*HttpEffectivePlacementQueryServiceTests'` | 158 passed, 0 failed/skipped |

Browser N/A for this documentation follow-up: no application/browser source, dependencies, generated assets, discovery, build/runtime inputs, commands, or test gates changed. Before committing this follow-up, the full unit command above passed all 3,593 cases and format verification passed again. Build, integration, and browser results are reused from `e1fc09bc`; the 1,225-file input inventory remains unchanged. The original R3 modified-click incident stays unresolved.

## Review and remaining limitations

No migration/model delta is introduced; existing model checks remain part of the full suites. Original #256 comp-comparison history and its user-approved actual-shell disposition remain unchanged; this is a scoped extension of that composition, with no newly approved comp or new fidelity score claimed. Separate review findings and their evidence-backed dispositions are recorded once in the linked local review; current execution results in this record supersede its contemporaneous pending-check notes.

The first desktop/phone capture round passed both browser journeys. One batched visual correction expands phone filters to full width, prevents table header word splitting, and makes scrollable history lists keyboard-focusable. The second capture round passes both journeys and supplies the four final captures. The changed-target detector ran once on five Razor/CSS targets and returned zero findings. It also reported the inherited #256 local `COMP_ROUND_OPEN` hero state; that older comp comparison and approved actual-shell disposition remain preserved, and no new comp fidelity pass is claimed for this scoped extension. The separate finish reviewer returned ship with no material fixes against the incumbent Fieldhouse system; a separate quality-bar card was unavailable.

The fresh documenter merged only the implemented Closed record and Campaign-Local Evidence Rule into DESIGN.md and its sidecar. JSON parsing, the bundled schema-version reader and DESIGN parser, explicit component-shape checks, exact rule matching, evidence links, incumbent-preservation assertions, and diff checks pass. The skill does not ship a full JSON-schema validator; none is claimed. No unrelated sidecar/token drift was changed and no shipping raster asset was added.
