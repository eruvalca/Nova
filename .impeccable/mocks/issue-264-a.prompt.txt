Create one high fidelity desktop web UI design reference for the Nova manual player intake board, OPTION A: single field sheet, as-built structure tightened. Use the attached actual current /players/new screenshot as the exact reference for the entire left shell (240 CSS px wide, Nova diamond, Nova Club operations, Dashboard, Club with wrapping name, Campaigns, Players active, Teams, bottom Manage and Logout), for the native system UI typography, and for the real tokens: teal #0E7C7B primary, pale sea glass #DDF2EC, ink #142F2E, pale background #F4F8F3, hairline borders. Keep EVERY shell item and their normal scale. Full viewport 1440x1000 CSS pixels, DPR1; output matching aspect and frame. Change only the main working field x300 to1380. At y48: Players 32px title left, teal Add player button right. At y120 a flat white 0.375rem-radius field sheet with one hairline border, no shadow, no nested cards. Inside it at y148 the heading 'Add player' 20px. At y184 one full-width bordered consequence band on pale sea glass reading 'With an Active campaign, this player joins Summer Tryouts immediately.' with the campaign name in semibold. From y250 two columns of labeled fields, label above input, 44px tall white inputs with hairline borders and 0.25rem corners: row one First name required and Last name required; row two Date of birth required and Graduation year required; row three Gender optional with a Not specified dropdown and Jersey number optional. Each label shows a small quiet required or optional word after it. Beneath each input an empty reserved line for a field error. At y600 a single teal primary button 'Create player' and an outlined 'Cancel' button beside it, both 44px, left aligned. Nothing else in the field: no summary rail, no metrics, no illustration, no photos, no eyebrow, no shadow, no gradient, no dark mode. Status colors always have words. This is an operational app screen, not artwork. The attached screen contributes real shell, real tokens and the real labels; its field ordering and spacing are tightened here.
