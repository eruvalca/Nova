FAIL hero 72% (drift) (record: nothing advanced)
  - hero overall 71.6% < 72% (structure 59%, color 96%, detail 48%)
  - region menu is missing (detail 0%, structure 79%): the comp shows material the build does not
  - region route is missing (detail 0%, structure 95%): the comp shows material the build does not
  - region readiness is missing (detail 11%, structure 72%): the comp shows material the build does not
  - region back is missing (detail 0%, structure 67%): the comp shows material the build does not
  - region identity is missing (detail 1%, structure 55%): the comp shows material the build does not
  - region local-outcome is missing (detail 0%, structure 68%): the comp shows material the build does not
  - region place is missing (detail 0%, structure 63%): the comp shows material the build does not
  - region traits-heading is missing (detail 0%, structure 52%): the comp shows material the build does not
  - region traits is missing (detail 0%, structure 70%): the comp shows material the build does not
  - region first-author is missing (detail 0%, structure 71%): the comp shows material the build does not
  - region first-time is missing (detail 0%, structure 69%): the comp shows material the build does not
  - region first-note is missing (detail 0%, structure 61%): the comp shows material the build does not
  - region second-author is missing (detail 0%, structure 81%): the comp shows material the build does not
  - region second-time is missing (detail 0%, structure 70%): the comp shows material the build does not
  - region composer-label is missing (detail 0%, structure 76%): the comp shows material the build does not
  - region save is missing (detail 0%, structure 43%): the comp shows material the build does not
  - region older is missing (detail 0%, structure 39%): the comp shows material the build does not
  - region participant-count (text) is contradicted (structure 48%, detail added 31%): the composition of this text region differs from the comp; re-derive it from the spec box
  - region season-team (text) is contradicted (structure 39%, detail added 38%): the composition of this text region differs from the comp; re-derive it from the spec box
  - region campaign-back: its ink sits in a 236x24px box in the comp and 158x21px in the build (padding, row height, or size); match the box, not only the position
  - READINGS, each one CSS edit (8 of 14, the rest after these):
  - text season-team: cap height 31.5px in the build, 17.8px in the comp (+77%); set font-size so the cap height renders at 17.8px
  - text traits-heading: cap height 8.2px in the build, 18.1px in the comp (-55%); set font-size so the cap height renders at 18.1px
  - text observations-heading: letter-spacing is wider than the comp's (gap 0.139 vs 0.086 of the cap height); set letter-spacing to close it by about 1px
  - text first-author: cap height 3.7px in the build, 18.3px in the comp (-80%); set font-size so the cap height renders at 18.3px
  - text first-note: cap height 8.2px in the build, 18.5px in the comp (-56%); set font-size so the cap height renders at 18.5px
  - text second-author: cap height 12.9px in the build, 18.5px in the comp (-30%); set font-size so the cap height renders at 18.5px
  - text second-note: the face renders 30% heavier than the comp's (ink density 0.58 vs 0.45); drop a weight step or use the ranked face
  - text participant-count: ink is #19282b in the build, #b0c0c0 in the comp; use the comp's colour
  - the build carries ink in 17 grid cells where the comp is calm (G2, I2, I4, F5, F6, G6, A7, G7, H7, I7, J7, A8, ...); nothing exists on the page that the comp does not show (a kicker, an extra nav item, a divider, a second row of controls); remove it or name it in a stated decision after the hero passes
  worst: save missing 39%; older missing 43%; traits-heading missing 49%
  open .impeccable\review\diff\hero\side-by-side.png
