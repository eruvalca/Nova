# PR #253 — round 12 independent local review

Reviewed complete production/test changes against `f44b1d66`, including both new NavigationProtection partials. Four exposed findings are corrected: selected participant links use `aria-current="page"`; failed discard storage writes preserve visible add/edit/trait drafts; unreadable Evaluate and drawer recovery now protects code-driven navigation as well as native links. Read-failure protection remains during retry and clears only on successful restoration or owner reset. Discard persists the intended empty snapshot before clearing fields, checks owner/request/revision, and ignores stale completion/error feedback.

Confirmed departure uses an owner/request/restoration-scoped permit only around actual navigation. Known pending operations and new drafts remain blocking. Drawer callback completion rearms the same-owner native guard, including callbacks that perform no navigation; failed history retains the existing single cancellation path. Replacement-owner cleanup cannot revoke a newer guard. Reviewed the 13 new component rows for direct form/NavigationManager entry, drawer close/previous/next, held retry, false/throwing writes, all three draft fields and edit version, newer drafts/owners, and no-op callback re-protection. Existing browser cases add real form submission under unreadable storage and current-page semantics without removing original preservation assertions.

Retracted an initial hypothetical delayed-WASM-callback concern after inspecting the saved ASP.NET NavigationManager, NavigationLock and navigation TypeScript sources: handlers are invoked synchronously before awaiting completion, NavigationLock immediately invokes the component callback, and .NET-originated JS navigation skips a second callback. The current guards complete synchronously; no destination metadata or parent callback contract expansion is warranted.

The fifth workflow-only location is cleanup lines 49–51; its full wording is unavailable in both downloaded logs and events, which expose only the aria-current stored comment. Independent source inspection finds no applicable production performance defect: all host contexts use Npgsql; PostgreSQL filters/orders/limits 500 receipts in SQL using the expiry-leading index. The non-Npgsql materialization is the documented SQLite DateTimeOffset fallback, also used by import cleanup. Existing provider tests cover expired removal and fresh receipt retention; operation timestamp expiry prevents deletion from restarting old mutations. No cleanup source change is necessary.

Reconsidered AGENTS and previously read Blazor, validation, service, API, tenancy and testing rules; add-blazor-ui lifecycle/interop, add-domain-persistence retry/query, nova-testing component/browser and assertion-quality recipes. Re-read service rules, domain skill/retention reference and relevant EF fallback instructions for the fifth location. No duplicate permanent rule is needed. This reviewer edited only this record and did not build, test, stage, commit or contact GitHub.

Inspected validated build: zero warnings/errors, 7.04s; targeted rerun: 284 passed, zero failures/skips, 2.508s. The initial CA1508 build failure and 283/284 targeted run remain recorded; the semantic capture predicate and production double-cancel correction preserve coverage. Full unit passed 3,172 (15.439s), full browser passed 183 (4m11.923s), and fresh PostgreSQL integration passed 608 (1m42.765s), all zero failures/skips. Browser source remained fixed; Aspire suites ran serially.

Verified both nine-file manifest hashes: initial `D4C99D54DDB7F1FCBAB6ABC216492FCDB23D1765E41CD4CCB48E226B146149F8`, final `DD8D0D27288F9FCDA1DC780E9288C776C9B0DB7CD601D9989E398BBC9E18D02C`, with every final file matching. Initial format found only the drawer recovery file's missing UTF-8 BOM. Independently hashing final bytes after removing the BOM reproduces its original manifest hash; this post-browser correction changes no source content. Final build passed with zero warnings/errors (7.05s); inspected final format tail, 0 of 949 files changed, 99550ms, with exit 0 directly observed by parent. No pending evidence or actionable finding remains in this bounded review.
