# PR #253 — review round 6 local review

## Initial independent assessment

Read-only review against `1a9cb8bed98d251a3b6ca69e510411ca8e058e5d`. The automatic review failed its file-count limit; its workflow log still records four withheld locations. Full finding text was not available. These conclusions derive from the located source, sibling implementations and actual recovery contract, rather than guessed wording. The user subsequently requested addressing visible findings without committing and stopping the watch; this reviewer performs no GitHub operations or commits.

## Concrete findings

1. **Medium — generic problem status cannot settle an unknown evaluation operation.** Evaluate `HandleRejectedCaptureAsync` clears retained pending state for every Validation/Forbidden/NotFound/Conflict. Drawer `CallStoredAsync` clears storage on the same broad status set. `EvaluationMutationExecutor` checks current membership before receipt lookup and rejects expired operation IDs before looking up receipts. Consequently, an operation can commit, lose its response, then receive Forbidden when replayed after membership removal. Both surfaces discard its original operation identity even though the receipt was never checked. Rejoining and saving the retained note as a fresh operation can duplicate the earlier commit. Expiry and operation-fingerprint conflicts likewise do not establish noncommit. Both located findings are manifestations of this shared invariant; inspected siblings include all Evaluate pending kinds and the drawer replay switch.

   Recommended correction: settle a failed command only from an explicit, trustworthy evaluation noncommit disposition bound to its original operation. Generic authorization/expiry/fingerprint failures retain the exact operation and payload. Preserve ordinary stale-note and Closed-campaign correction flows with a positive noncommit result; do not infer disposition from human-readable text. Existing structured `ServiceProblem.Extensions` can carry the contract through server and WASM without inventing a parallel error transport. The corrective source and regression dispositions are recorded below.

   **Concurrency refinement:** absence of a receipt under a lock alone is insufficient to make rejection terminal. A prior ambiguous request may still be queued before that lock. Retry B can observe Closed and return rejection; after reopening, delayed original A can acquire the lock and commit. Therefore a claimed terminal rejection must itself be durable for the actor/operation/fingerprint, so all delayed identical requests recover the same rejection. The existing receipt's JSON can carry an internal success/problem envelope without a schema change in this pre-release repository. A mutation savepoint permits rollback of partial effects before recording rejection in the outer transaction; preserve membership/roster locks acquired before the savepoint and discard tracked mutation state. Provider evidence should cover a delayed same-ID attempt across rejection and reopen, not only sequential rejection replay.

2. **Low — original validation is labeled latest.** The implementation README's table labels the `381d5019` results “Latest result” and lists original counts 2,946/598/148. Its preceding paragraph correctly says subsequent round records supersede those checks, but the table's label is contradictory. Label these as original implementation evidence and keep current review-round links authoritative. The nearby blanket statement that no diagnostic suppression was added also needs historical scope because later records explicitly document narrow CA1812 reflection exceptions. This is documentation accuracy, not evidence that the final local suites failed.

3. **Capture-scope convention — no applicable code change found.** As in round 5, stable capture scope is authenticated user plus club, while authority scope separately includes administrator role. Both Evaluate and Roster drawer consume the same capture scope and append campaign/participant. Role changes invalidate visible authority without stranding the original recovery payload. The inspected convention location does not establish a defect, and unavailable full wording cannot justify a speculative refactor.

## Initial corrective-diff inspection

The initial corrective production diff persists an internal success/problem envelope in the existing receipt JSON. Rejected mutation effects roll back to a savepoint, tracked mutation state is cleared, and rejection receipt insertion/commit remains inside the outer membership/roster lock scope. Recovery returns the original rejection; ambiguous-commit verification uses that same recovery path. Prelookup authorization, expiry and fingerprint rejection remain unmarked. Expiry checks still prevent old UUIDv7 identities from becoming fresh mutations after receipt cleanup.

The shared marker accepts only supported rejection kinds and an exact original operation identity, including strictly parsed HTTP JsonElement/string representations. Both surfaces now clear retained state only for successful receipts or matching noncommit proof. Unmarked Evaluate problems retain pending data while refreshing evidence. Existing shared HTTP conversion preserves the marker alongside structured errors and trace correlation. The README now labels original results and scopes its suppression statement historically. The final executor also keeps ServerError nonterminal after rolling back its effects, addressing the defensive boundary noted during initial inspection. No remaining actionable source or test findings in the final bounded diff; final validation has an unresolved full-browser limitation described below.

## Sources and limits

Assertion-quality inspection of the new UI and shared-marker regressions found no assertion-free or trivial-only methods. UI assertions combine rendered copyable/readonly state, storage side effects, exact original payload equality across reload, and successful recovery; the corrected-edit flow pins deliberate review of the current version and a new operation identity after proven rejection. Shared helper cases reject malformed/wrong/empty marker values, preserve errors and trace data without mutating the source problem, and exercise actual HTTP JSON decoding. Separate terminal-expiry cases now use old UUIDv7 identities, retain copyable text and pending state across reload, reject fresh Save, and verify only exact original operations are called; they do not simulate eventual success after expiry.

The provider delayed-original case gates a real command before membership locking, lets another same-ID request persist Closed rejection, reopens, then releases the original and asserts the same rejection, zero notes and one receipt. The commit-failure case requires an exception and zero receipts/effects before retrying the same ID successfully after reopening. The lost-acknowledgement case injects a postcommit failure, requires fresh-context recovery and verifies the negative receipt remains terminal after reopen. The partial-effect case flushes a note, confirms it is visible inside the attempt, rejects, then verifies zero notes and one receipt outside the transaction and only one mutation-delegate invocation across replay. The real HTTP case commits, removes persisted membership while retaining the cookie, verifies unmarked Forbidden, reinstates membership and recovers the exact original receipt with one note/receipt. These verify behavior rather than mirroring the serializer formula.

Final source contains 33 new unit cases and five new integration cases (four provider plus one HTTP). Nine preliminary unit failures were existing fixture assumptions: eight receipt counts and one generic validation mock. Inspected corrections retain no-evidence/no-activity assertions, assert positive operation-bound rejection where appropriate, and count intentionally durable rejected receipts. They do not weaken authorization or convert failures to skipped checks. All corrected source/tests are reviewed; final command evidence and fingerprint are recorded below.

Inspected actual Evaluate rejection/commit/retry paths, drawer retained call/replay and navigation protection, `EvaluationMutationExecutor` validation/authorization/receipt lookup/expiry/transaction order, `EvaluationNoteService` stale-version and author checks, shared `ServiceProblem` and server/client extension transport, workspace scope construction and both child bindings, the located README and prior round evidence. Read the round 6 workflow location records directly. Reused previously read AGENTS and C#/Blazor/API/validation/service/tenancy/testing rules and feature recipes; reread the recoverable-command rules, `add-blazor-ui` lifecycle/state recovery reference, API validation/ProblemDetails reference and persistence retry/lock reference for this assessment. Applied the installed `assertion-quality` skill, its `test-analysis-extensions` routing source and .NET assertion reference to the requested semantic test review. Earlier local review records retain the full guidance inventory.

## Final validation evidence

Independently read `round6-build-final.log`: build succeeded, zero warnings/errors, 1m 08.87s. Read `round6-unit-final.log`: 3,099 passed, zero failed/skipped, 55.004s. The preliminary nine fixture failures described above remain historical evidence; this final run includes their corrections.

Independently hashed `round6-source-manifest.log`: SHA-256 `77CF6074EFAFC823BE31054D807070B7B6D96A7ABC60582E7E3C6CB62590B776`, base `1a9cb8bed98d251a3b6ca69e510411ca8e058e5d`. Verified all 20 listed current application/test file hashes, including untracked files, with zero mismatches. This identifies uncommitted source without staging it. Later validation results are recorded below.

The first integration run, `round6-integration.log`, retained four older fixture failures (605 total, 601 passed). Inspected the two subsequent integration-only fixture changes: tag close/player-archive/tag-archive races now require exact noncommit proof plus one receipt, whereas membership denial still requires no proof and zero receipts; each retains the assertion that no tag application was created. Draft removal's real HTTP test now asserts the exact operation marker and one rejection receipt while verifying the application remains. These are appropriate contract updates, with no weakened evidence-authorization assertions.

The expanded final manifest, `round6-source-tested-manifest.log`, hashes to `0EF79F09AF2D2EE6D2FEDACF2CFB0FFBF5F6AC26BB4764650A01432DBDCC6093`; independently verified all 22 current file hashes with zero mismatches. The earlier 20 application/unit files are unchanged. Read `round6-build-tested.log`: zero warnings/errors, 42.11s. Later validation results are recorded below.

Read `round6-integration-final.log`: 605 passed, zero failed/skipped, 3m 17.189s. The full browser run in `round6-browser.log` has 171 passed and one failure: `ThousandParticipantLookupRemainsBoundedAndRecordsSeparateBenchmarkAsync` timed out after five seconds waiting for initial Save-note readiness at line 70. This precedes any mutation and the lookup benchmark. The test uses a fresh signed-in browser context and campaign. The Save button renders under `Writable` and is disabled only by pending recovery or unavailable storage readiness; startup attachment, storage adapter, markup and this test are unchanged in round 6. No rejection/receipt path has executed at the failure point, so the failure does not directly implicate the new contract. The limited button-only diagnostic cannot distinguish delayed attachment from import/storage failure, and does not establish a product root cause or a passing scale benchmark. The failed full run is retained and not represented as passing.

Independently read `round6-browser-responsive-recheck.log`: both unchanged responsive cases passed, zero failed/skipped, 50.430s. This verifies the targeted scenarios but does not itself replace the full suite. `round6-format-final.log` is empty; the implementer reports tool session 95006 completed with exit 0, so the exit status is attributed to that observed tool result rather than inferred from an empty file. The saved 22-file manifest hash remains unchanged. The final unchanged full-browser recheck result follows.

Independently read the completed `round6-browser-final.log`: the unchanged full suite again passed 171 and failed the same initial-readiness case, with zero skips, 5m 56.518s, exit 2. The recurrence under the full suite while the focused class passes is an unresolved startup-readiness validation limitation; the root cause remains unproven. No timeout, assertion, startup source or test was changed to hide the failure. The scale benchmark is not credited as passing within either full run. Per the user's narrowed scope, the current review fixes remain uncommitted and the separate readiness investigation is left explicit rather than silently expanding the task.

## Authorized cleanup and readiness follow-up

The user subsequently authorized cleanup and one combined push. Independently reviewed the one-file responsive-test correction: both scenarios now use the existing bounded hydration interaction helper to enter a probe into an editable composer, require its bound character count, fail on visible region errors, clear the probe and still require Save enabled. This establishes actual component input handling before captures or the lookup benchmark; it does not submit evidence or replace benchmark assertions. Constants are unchanged, but the effective startup allowance is broader than the prior single five-second Save wait: it uses the shared default 60 attempts with 250ms delays and bounded actions before the original final Save assertion. This change must be described as adopting the shared readiness policy, not as unchanged timing. The resulting build/browser evidence is recorded below.

Read the retention addition in AGENTS, both Impeccable skill copies and `.gitignore`. The rule distinguishes durable decisions/final evidence from intermediate outputs, requires a shared archive with revision/checksum before untracking and explicitly preserves validation gates. Both ecosystem copies carry identical behavior. The pinned prior Git tree is an appropriate shared archive; a local ZIP alone would not be. Preexisting tracked evidence is deliberately outside this cleanup. The narrow reuse concern is resolved by explicit AGENTS guidance to add specific ignore exceptions for each approved comp/provenance package and avoid force-adding generated directories.

Independently verified `artifact-archive.json` contains exactly the 141 staged deletions, all 141 local SHA-256 values and pinned `1a9cb8be` Git blob IDs match, and the local ZIP matches recorded SHA-256 `080E7F9090680DA082792A6612F4DEE26A82CCB74EE7B3A4CB83262664E62B9A`. Compared both skill retention paragraphs directly; they are identical. The README links the pinned shared tree and manifest and explains historical JSON/prose paths. Its authorization sentence and the watch record now correctly state the subsequent single-push authorization while monitoring remains stopped. Read `round6-push-build.log`: build succeeded with zero warnings/errors, 2m 54.86s.

Read `round6-push-unit.log`: 3,099 passed, zero failed/skipped, 1m 55.180s. Independently hashed `round6-push-source-manifest.log` as `A8B588DB191E8514A914F2F0CE7EF96AC6163E6AAC4F0F95769D420355309F89` and verified all 23 current application/test file hashes, including the changed responsive test, with zero mismatches.

The subsequent full browser attempt, `round6-push-browser.log`, reports three failures: the Roster page-boundary navigation case, the lost-response case's WebAssembly attachment prerequisite, and non-square club-crest upload's onboarding prerequisite. It has no final summary or reliable completed count; the implementer canceled the stalled runner after roughly 15 minutes and reports its owned processes exited. This attempt is not a passing full suite. The browser gate remains unsatisfied and must remain unchecked in PR validation; the prior integration and unit passes do not substitute for it. Independently read the changed responsive-class recheck, `round6-push-responsive.log`: two passed, zero failed/skipped, 1m 45.347s. The final `round6-push-format.log` is empty; the implementer observed execution session 2098 exit 0, which is the source of the format-success claim rather than the absence of diagnostics. All attempted checks now have recorded outcomes; the incomplete failed full-browser attempt remains an explicit limitation.

No builds, tests, source edits, staging or GitHub requests were run by this reviewer. Only this review record is owned here. Recovery, readiness-probe and retention source review is complete with no remaining actionable static finding. The full browser gate remains unsatisfied; authorizing a push does not establish merge readiness or resume monitoring.
