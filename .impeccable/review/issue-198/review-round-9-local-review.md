# PR #253 — round 9 independent local review

Reviewed actual source and regression changes against `fae6ed6753339174b6d58534b84157eb996fd480`, with the two distinct stored findings from the archived automatic-review session. The revised duplicate Place wording is one finding.

- **Place handoff: fixed.** During a row save the old code queued only filter/page state; completion omitted applied selection and focus, so a later identical parameter render reloaded again. The tuple now retains state and participant together. A shared helper applies both and the linked-heading focus flag in immediate and deferred paths. Existing latest-request replacement, return-to-applied cancellation, save guards and loading protection remain intact. Inspected filter/page/retry, save-finally and normal parameter siblings.
- **Relevance coverage: addressed by new regression matrix.** Existing tests covered the requested sort and strict HTTP payload validation but did not execute server ordering before paging. New SQLite and PostgreSQL cases exercise Active fast/filtered and Closed reads with 23 matches. The exact tryout match lies at name position 23; baseline excludes it from page one, relevance promotes it first, and combined 20+3 pages assert full name/assignment tie order. No production ordering change was necessary.

Handoff regressions hold save and reload separately, replace the requested selection, verify no early reload/focus, assert the selected participant query and exactly one focus, and rerender unchanged parameters to reject redundant reload/refocus. A return-to-applied case cancels the pending request. Query regressions assert real service results and exact participant identities rather than a mocked order. The final SQLite arrangement reverses player IDs for two same-name assignments, distinguishing preserved Roster player-ID ordering from relevance assignment-ID ordering; the expected sequences are independently asserted.

Applied previously read AGENTS review rules, C#/Blazor/testing/tenancy/placement guidance, add-blazor-ui lifecycle ownership and nova-testing component/SQLite/PostgreSQL recipes. No new permanent rule is needed for these already-documented invariants. This reviewer made no source/test edits or execution calls; only this record is owned here.

Final source/test review is complete with no actionable static finding. Independently verified all six file hashes and manifest SHA-256 `08F105C3BBC22A6BBC5F0AC824749077F396FB587DD8E962D6E86DA973D22135` in `.impeccable/archive/round9-source-manifest.log`. Inspected `round9-build.log` (zero warnings/errors, 2m43.71s), `round9-unit.log` (3,132 passed, zero failed/skipped, 40.943s), and `round9-integration.log` (608 passed, zero failed/skipped, 1m32.204s). Full browser and final format remain pending; the earlier incomplete full-browser gate is not treated as passing.

The first browser run subsequently completed 174/176, two failed, zero skipped, 10m32.208s (`round9-browser.log`, summary inspected). Diagnosed a production loop: persistent Closed-history Conflict prompted lifecycle reconciliation, which removed the shell and recreated the selected drawer/context, whose initial Conflict restarted reconciliation. The shared parent guard now coalesces nested callbacks and keeps regions mounted; unchanged context keys settle into regional error/retry. Real lifecycle/authority changes still invalidate context. Reviewed readiness, parameter ownership and retry siblings. New regression asserts the same drawer instance, exactly two detail reads across async results/rerender, and later explicit retry recovery; its four-read failure safety bound cannot satisfy the two-read assertion. Existing Active-to-Closed coverage verifies old rows clear and read-only context appears.

The second browser failure was the five-second post-reload receipt-restoration prerequisite. The new wait only observes restoration and terminates on errors or unexpectedly editable state; it never types, clears or dispatches. Exact original text/payload and one-note assertions remain. The broader existing bounded policy is a reviewed functional-readiness allowance, not a latency SLO. Follow-up source/test inspection has no actionable finding; updated build/suite/format evidence remains pending.

The first follow-up build failed MA0051 (41 statements against 40) in the lost-response scenario; inspected `round9-final-build.log`, one error, zero warnings, 6m30s. Grouping document reload and its restoration wait into `ReloadRetainedCaptureAsync` preserves their order and every assertion without suppression. Reviewed that extraction and independently verified all ten final source hashes plus manifest SHA-256 `9D939814A56387A137D82EA78CBAD57ED509A3BEA4A0BEFC7023A0AC9F388BE3`. Final execution remains pending.

Verified the four newly archived round 7/8 records: local SHA-256, canonical blobs at immutable `fae6ed6753339174b6d58534b84157eb996fd480`, absence from index, and ignore matches. Retained README/watch/archive links resolve. The evidence index now covers 48 historical paths across its two pinned revisions; no source/tests were removed to meet review size.

Final logs inspected: `round9-final-build-retry.log` passed with zero warnings/errors (1m01.65s); `round9-final-unit.log` passed 3,133/3,133 (1m41.140s); `round9-final-integration.log` passed 608/608 (6m16.355s); `round9-final-browser.log` passed 176/176 (9m30.361s). All suites report zero failed/skipped. Reverified the unchanged ten-file final manifest. This clean complete browser run supersedes the earlier incomplete gate without erasing retained failures. Reviewed the narrowed historical ignore patterns and confirmed round 9 and future round 10 records are not ignored. Inspected final format log: 0 of 946 files changed, completed in 445650ms; parent directly observed exit 0. All final checks passed, no result remains pending, and no actionable source finding remains in this bounded review.
