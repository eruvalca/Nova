# PR #253 — review round 7 local review

Primary-agent completion addendum: full format verification subsequently passed
(exit 0, `round7-format-final.log`). Three new C# files received only BOM fixes and
one formatter-required line break after test execution. The reviewed pre-format
20-file manifest and logs remain identified below; final mechanically formatted
manifest is `6B0F111E38E43E57391E5081D529FCF7B2D2EA8F62BEF76E96C328E17362E44B`.
The primary agent checked those mechanical changes. This addendum is not a claim
that the separate reviewer executed additional tests or verified the later hash.

## Initial independent assessment

Read-only assessment against `f76ae55738145587cec09b272366c7441ea59901`. The user explicitly resumed monitoring. Read posted storage-scope comments from the archived thread response and the workflow's additional stored locations. Full wording for the JavaScript/reset candidates was unavailable; conclusions below derive from actual source and sibling behavior. The implementer owns the corrective changes; the final production and regression diff has now been independently inspected.

## Findings and dispositions

- **Storage-scope suggestions — inapplicable.** Both posted comments request adding permission state to the persisted recovery key. Evaluate and drawer intentionally use stable authenticated user/club plus campaign/participant for storage, while visible state and in-flight ownership use authority scope including administrator role. Their parameter paths reset and reload authorized evidence when authority changes. The server rechecks persisted membership before receipt recovery; uncommitted commands recheck participant visibility, lifecycle and operation-specific author/administrator requirements. A prior role does not confer write permission through stored input. An immutable committed receipt may appropriately recover for its original actor after demotion. Changing the key would strand the original operation and invite a fresh identity after an ambiguous commit, violating recovery requirements. The posted suggestion should be explained rather than applied. A narrow clarification in the shared recovery guidance can distinguish visible-authority invalidation from stable storage identity.
- **Medium — prior identity failure survives participant reset.** `ResetParticipantOwner` clears notes/application/choice errors but leaves `_identityError`. When A's identity read fails, selecting B while B's request is held renders A's error and Retry identity in B's sheet. Clear identity failure at the owner boundary; test the intermediate held request rather than only the eventual success. The drawer's corresponding detail-load path already resets `_detailError` before awaiting.
- **Related reset gap — stale trait-removal confirmation.** Evaluate leaves `_removeApplicationId` across owner changes. A→B→A can resurrect A's previous inline removal confirmation without reopening it. The drawer already clears its removal ID and confirmation during owner reset. Clear the Evaluate confirmation along with picker/search state under the implementer's trait-draft work. This does not authorize dropping a dispatched retained operation.
- **Located JavaScript validation — no additional defect established.** At the supplied lines, `read` marks unknown storage protected, distinguishes absent storage from malformed JSON, checks required snapshot/operation shapes and leaves original bytes untouched on rejection. C# then validates kind-specific inputs and assignment identity before publishing storage readiness. Missing/falsy pending forms are rejected explicitly. The unavailable review wording is not grounds to invent a different allegation. The final trait-draft shape and protected native-input changes were also reviewed.

## Final corrective-diff review

The current patch separates authoritative identity/lifecycle readiness from add-note, apply-trait and placement capabilities. Edit/delete/removal dispatch checks use the current loaded row's capability; both recent and expanded note renderers use their row permissions, including already-open editors. Retained-operation replay remains independent of new-action UI capabilities and reauthorizes on the server.

Trait text now participates in the capture snapshot, storage validation/restoration/retry, owner reset, draft/departure checks and explicit discard. Closed or revoked apply capability leaves the entered label copyable without write controls. A matching committed create-and-apply clears only its own label. Applying an existing trait snapshots the current lookup into the retained pending payload and clears that matching lookup after settlement while preserving neighboring note text. The shared JavaScript guard detects marked input values before Blazor updates its protection attribute, covering the native interaction interval. No other navigation protocol behavior changed.

The reset corrections clear identity failure/loading state and old removal confirmation/picker state before new-owner reads. Both stable-key comments and the single canonical Blazor instruction now explain why permission changes invalidate visible authority without changing unresolved-operation storage identity. The clarification is available to both ecosystems through existing instruction routing. No remaining actionable source finding. The current-row capability guard also covers version review and captured stale callbacks; retained dispatched recovery stays separate from new action authorization.

## Sources and limits

Read actual Evaluate interop attachment and storage adapter, participant reset/lifecycle, evidence/prerender state, mutation validation and shared navigation guard; drawer storage, authority ownership and reset paths; workspace capture/authority bindings; current executor membership/receipt/operation authorization behavior; archived review comments and round 7 workflow locations. Reused applicable AGENTS, Blazor/API/validation/service/tenancy/testing rules and the previously read feature recipes, including lifecycle ownership and durable recovery guidance. Reconsidered applicability for the new storage/reset locations; earlier records retain the detailed guidance inventory.

This reviewer initially performed read-only source/test review. After the first full browser run, the implementer explicitly delegated the diagnosed ClubCrestBrowserTests.cs correction here; that narrow test patch is implementation by this reviewer and requires the parent's independent check. No production edits, builds, test runs, staging, commits or GitHub requests were performed here. Test-file ownership has been returned. Execution and the final tested-source identity remain pending.

## Regression review

Read both new unit partials (`CampaignEvaluationPanelTests.Capabilities.cs` and `CampaignEvaluationPanelTests.TraitDraft.cs`), required-trait snapshot rejection rows and related fixture updates, plus both changed browser protocol/storage tests. The 27 new unit cases cover independent add/apply/place combinations, row-authorized edit/delete/removal when unrelated identity capabilities are false, captured callbacks after capability or lifecycle revocation, trait-only navigation protection/discard, Active/Closed restoration, failed storage retry, neighboring note preservation, matching apply settlement, lost create response followed by exact-operation replay, and intermediate owner-reset states. The four browser rows extend actual shipped-module checks for missing/null/non-string trait text and native trait input before any Blazor rerender.

Assertions verify observable controls and copyable text, absence of unauthorized service/storage calls, exact version/operation payloads, unchanged retained bytes, and navigation protection/revocation. These exercise the violated invariants rather than merely duplicating implementation expressions. Existing capture fixtures explicitly include the newly required trait field; no compatibility fallback was introduced. No assertions were weakened, test rows skipped, timeout limits raised, or diagnostic suppressions added in this round's inspected test diff. Validation results will be recorded when supplied; source inspection alone is not a test-pass claim.

## First execution and browser correction

The first full browser run (`round7-browser.log`) completed with 176 cases: 171 passed, five failed, zero skipped in 10m57.110s. The failures were the Roster boundary variant, lost-response WebAssembly attachment, two club-crest creation scenarios, and Closed roster reload. This is not a passing browser gate. The parent and test agent own the other failure diagnoses and corrections.

Inspected both crest failure logs and the actual CreateClubForm bindings/state. The local warmup wrapper had no positive attachment probe; it waited only for a visible input, then both scenarios filled the three required fields once before their retrying upload. The log subsequently showed a saved crest and all three local Required messages. The component holds one readonly form model; crest selection, save and clear never reset its Name/City/State. The concrete test precondition defect is that field events can precede attachment while only the later file event is retried until the live cropper responds. Retrying Create Club cannot redeliver those earlier text events.

Under explicit delegation, changed only ClubCrestBrowserTests.cs: both creation scenarios fill fields after the successful crop-save/preview interaction, then explicitly blur State with Tab. Existing crop, creation, navigation, image and aspect assertions remain unchanged. Corrected misleading attachment comments; no timeout or retry budget changed. This is test implementation by this reviewer, not independently reviewed by the same author; the parent is responsible for its separate check. The file's whitespace diff check passed, but build and execution of the corrective test patch remain pending.

The parent independently inspected and approved the crest patch. Separately reviewed the test agent's extracted EvaluationInteractionHelpers and Capture/Responsive callers, plus the drawer boundary correction. The helper retains the former responsive handled-input/count/clear/Save checks and terminal alert handling. For the WASM reload caller, the effective startup allowance broadens from a bare five-second Save assertion to the existing bounded hydration policy; this is an explicitly reviewed functional-attachment correction, not proof of a latency SLO. The drawer probes Add note/Cancel after full navigation, then sends Next once and observes exact position without waiting on a temporarily absent locator or repeating the move. Existing sequence, URL, heading and boundary assertions remain. Flagged the sibling initial bare Save assertion in the same lost-response case for the parent's disposition. The Closed reload timeout remains unexplained; no source or test workaround was justified by the available evidence.

The sibling initial Capture readiness check now also uses the shared composer probe, resolving that finding. The next full run (`round7-browser-final.log`, summary inspected) completed with 175 passed, one failed, zero skipped in 4m59.060s; the parent reports all five prior failed cases passed. The sole remaining failure was the keyboard test focusing Add note before recovery enabled it. Independently checked the follow-up correction against the actual disabled markup and separate close-focus/recovery paths: it resolves and focuses the enabled button through the existing bounded policy, observes actual activeElement, and retains the original focused assertion, Enter activation, entered content, Save activation, status and persisted-row checks. No actionable static finding. The effective readiness allowance changes as documented above; keyboard behavior itself is still asserted. Corrective build, focused verification, final format and source fingerprint remain pending. A complete passing browser run has not yet been established.

## Final evidence inspected

Independently hashed all 20 application/test files in `round7-final-source-manifest.log`: zero mismatches. The manifest SHA-256 is `CC381131C0DE90AFFDD8D0186E432F89662AD38356EC63D1A6E82159B937490B`, against base `f76ae55738145587cec09b272366c7441ea59901`.

- `round7-unit.log`: 3,126 passed, zero failed/skipped, 1m37.196s. The parent confirms no production or unit-test changes after that run; later corrections affect browser tests only.
- `round7-keyboard-build.log`: build succeeded with zero warnings/errors, 11.72s.
- `round7-browser-focused.log`: the complete CampaignEvaluationBrowserTests class passed 20/20, zero failed/skipped, 1m41.375s, including the corrected keyboard case.
- Both full browser attempts above remain retained failures; their summaries were inspected. Focused success does not establish a passing full-suite gate. A clean full browser run remains required before merge.
- Final format verification remains pending; no successful result is inferred from an empty log.

No outstanding actionable source finding in this bounded review. No execution was performed by this reviewer. The final format result is the only awaited command evidence for this round's record; the full browser validation limitation remains explicit.
