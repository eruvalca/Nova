@* Shared submit button: primary action leads, quiet actions follow; full width on narrow screens. *@
<button type="submit" class="account-submit btn @ButtonVariant @(Block ? "w-100" : null)" disabled="@Disabled">
    @Text
</button>
